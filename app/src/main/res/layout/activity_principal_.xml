<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_principal_"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.raitom.sqlitecrud.ui.Principal_Activity">

    <Button
        android:text="Clientes"
        android:layout_height="200px"
        android:id="@+id/clientes"
        android:layout_marginTop="30dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginLeft="100dp"
        android:layout_marginStart="100dp"
        android:layout_width="300px" />

    <Button
        android:text="Prestamos"
        android:layout_width="wrap_content"
        android:layout_height="200px"
        android:id="@+id/prestamo"
        android:layout_marginTop="50dp"
        android:layout_below="@+id/clientes"
        android:layout_alignLeft="@+id/clientes"
        android:layout_alignStart="@+id/clientes"
        android:layout_alignRight="@+id/clientes"
        android:layout_alignEnd="@+id/clientes" />

    <Button
        android:text="Pagos"
        android:layout_width="wrap_content"
        android:layout_height="200px"
        android:id="@+id/pagos"
        android:layout_marginTop="40dp"
        android:layout_below="@+id/prestamo"
        android:layout_alignLeft="@+id/prestamo"
        android:layout_alignStart="@+id/prestamo"
        android:layout_alignRight="@+id/prestamo"
        android:layout_alignEnd="@+id/prestamo" />
</RelativeLayout>
